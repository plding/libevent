CC = @CC@
CFLAGS  = @CFLAGS@ -I../
LDFLAGS = @LDFLAGS@

PROGS = event-test

all: $(PROGS)

event-test: event-test.c
	$(CC) $(CFLAGS) $(LDFLAGS) $< ../libevent.a -o $@

clean:
	rm -f $(PROGS) *.fifo *.o *.out

distclean:
	rm -f Makefile
